# 🌍 Environment (use 'dev' or 'prod')
DJANGO_ENV=dev

# -----------------------------
# ⚙️ General Django Configuration
# -----------------------------

# ⚠️ Set to False in production!
DEBUG=True

# 🔐 Django secret key (keep this secret!)
SECRET_KEY=django-insecure-g9uc27-cjgdl7&nk@sk@o23!=0ntwa-7u)+%tgk+p7&zmt(k3g

# 🌐 Comma-separated list of allowed hosts (no spaces!)
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------
# 🗄️ Database Configuration
# -----------------------------

# 📦 PostgreSQL (used in production)
# Format: postgres://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=postgres://postgres:password@localhost:5432/restora

# -----------------------------
# 🔁 Redis / Celery Configuration
# -----------------------------

# 🔃 Redis for Celery broker and result backend
REDIS_URL=redis://localhost:6379

# -----------------------------
# 📬 Email Configuration (used in production)
# -----------------------------

# 📡 SMTP host (e.g. smtp.gmail.com, mailgun, etc.)
EMAIL_HOST=smtp.yourmail.com

# 📤 SMTP port (587 for TLS)
EMAIL_PORT=587

# 👤 Your email address (usually login username)
EMAIL_HOST_USER=you@example.com

# 🔑 App-specific password or real email password
EMAIL_HOST_PASSWORD=yourpassword

# ✉️ Default from address
DEFAULT_FROM_EMAIL=you@example.com
