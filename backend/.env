# -----------------------------
# ⚙️ General Django Configuration
# -----------------------------

# ⚠️ WARNING: Set DEBUG to False in production!
DEBUG=True

# 🔐 Django secret key (keep this safe and out of public repos!)
SECRET_KEY=django-insecure-g9uc27-cjgdl7&nk@sk@o23!=0ntwa-7u)+%tgk+p7&zmt(k3g

# 🌐 Hosts that are allowed to access the app (comma-separated, no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------
# 🗄️ Database Configuration
# -----------------------------

# 📦 PostgreSQL database URL in the format:
# postgres://<user>:<password>@<host>:<port>/<dbname>
DATABASE_URL=postgres://postgres:password@localhost:5432/restora

# -----------------------------
# 🔁 Redis / Celery Configuration
# -----------------------------

# 🔃 Redis URL used as the broker and backend for Celery
REDIS_URL=redis://localhost:6379

# -----------------------------
# 📬 Email Configuration (SMTP)
# -----------------------------

# 📡 SMTP server address for sending emails
EMAIL_HOST=smtp.yourmail.com

# 📤 SMTP port (587 is common for TLS)
EMAIL_PORT=587

# 🧑 Sender email address (usually same as login username)
EMAIL_HOST_USER=you@example.com

# 🔑 App-specific password or email account password
EMAIL_HOST_PASSWORD=yourpassword
